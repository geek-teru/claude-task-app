# 2-11. コード・CLAUDE.md レビュー

## レビュー結果サマリ

| 分類 | 件数 | 重要度 |
|------|------|--------|
| 修正済み | 3件 | ER図FK表記、テスト方針重複、プロジェクト構造 |
| 将来対応 | 1件 | 論理削除フィルタの共通化 (Phase 3 以降) |
| 問題なし | 2件 | toModel/toEntity パターン、getEnv 重複 |
| 致命的欠陥 | 0件 | — |

## 修正済み

### 1. ER図の FK 表記 (重要度: 中)
- **現状**: `user_id FK` と記載されていたが、外部キー制約不使用の方針と矛盾
- **対応**: `user_id FK` → `user_id` に変更。リレーション線で関係を表現

### 2. テスト方針の重複 (重要度: 中)
- **現状**: 「コーディング規約 > バックエンドテスト」と「テスト方針 > テストデータ / テスト実装パターン」に同内容が重複
- **対応**: 「テストデータ」「テスト実装パターン」セクションを削除し、「コーディング規約 > バックエンドテスト」に統一

### 3. プロジェクト構造の記載漏れ (重要度: 低)
- **現状**: `cmd/migrate/` が未記載
- **対応**: プロジェクト構造に `cmd/migrate/` を追記

## 追加対応

### 外部キー制約の廃止
- model.go から FK 定義 (`User UserModel`) を削除
- database.go に `DisableForeignKeyConstraintWhenMigrating: true` と `dropAllForeignKeys` を追加
- CLAUDE.md の DB 設計ルールに方針を明記
- テストから FK 依存のテストケース（存在しないユーザーID）を削除

### Skills 作成
- `/pm` — Phase のオーケストレーション
- `/engineer` — タスク実装 + 作業記録
- `/reviewer` — レビュー + レビュー記録
