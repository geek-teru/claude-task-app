services:
  db:
    image: postgres:16
    container_name: claude-task-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: password
      POSTGRES_DB: claude_task_app
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
